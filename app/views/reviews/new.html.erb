<div class="container">
  <div class="row"><%= simple_form_for [ @restaurant, @review ] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: 0..5 %>
    <%= f.submit "Submit review", class: "btn btn-primary" %>
  <% end %>
  </div>
  <%= link_to "Back to list", restaurants_path %>
</div>
